import styled, {css} from "styled-components";

export const AppWrapper = styled.div`
    display:flex;
    width: 100%;
    min-height: 100vh;
    padding: 2rem;
    background: #141414;
    justify-content: center;
    position: absolute;
    top:0;
    z-index: -2;
  `
